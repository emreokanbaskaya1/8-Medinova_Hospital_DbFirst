@model List<Medinova.Models.Doctor>

@{
    ViewBag.Title = "Doktor Yönetimi";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h4 class="card-title">Doktor Listesi</h4>
                    <a href="/Admin/AdminDoctor/CreateDoctor" class="btn btn-primary float-right">
                        <i class="fa fa-plus"></i> Yeni Ekle
                    </a>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Resim</th>
                                    <th>Ad Soyad</th>
                                    <th>Departman</th>
                                    <th>Açýklama</th>
                                    <th>Ýþlemler</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model != null && Model.Any())
                                {
                                    foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>@item.DoctorId</td>
                                            <td><img src="@item.ImageUrl" alt="@item.FullName" style="width: 50px; height: 50px; object-fit: cover; border-radius: 50%;"></td>
                                            <td>@item.FullName</td>
                                            <td>@item.Department?.Name</td>
                                            <td>@(item.Description != null && item.Description.Length > 40 ? item.Description.Substring(0, 40) + "..." : item.Description)</td>
                                            <td>
                                                <a href="/Admin/AdminDoctor/UpdateDoctor/@item.DoctorId" class="btn btn-warning btn-sm">
                                                    <i class="fa fa-edit"></i> Düzenle
                                                </a>
                                                <a href="/Admin/AdminDoctor/DeleteDoctor/@item.DoctorId" class="btn btn-danger btn-sm" onclick="return confirm('Silmek istediðinize emin misiniz?')">
                                                    <i class="fa fa-trash"></i> Sil
                                                </a>
                                            </td>
                                        </tr>
                                    }
                                }
                                else
                                {
                                    <tr>
                                        <td colspan="6" class="text-center">Henüz kayýt bulunmamaktadýr.</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
