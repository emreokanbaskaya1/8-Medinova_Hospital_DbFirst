@model Medinova.Models.Testimonial

@{
    ViewBag.Title = "Testimonial Güncelle";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h4 class="card-title">Testimonial Güncelle</h4>
                    <a href="/Admin/AdminTestimonial/Index" class="btn btn-secondary float-right">
                        <i class="fa fa-arrow-left"></i> Geri Dön
                    </a>
                </div>
                <div class="card-body">
                    <form action="/Admin/AdminTestimonial/UpdateTestimonial" method="post">
                        <input type="hidden" name="TestimonialId" value="@Model.TestimonialId" />
                        <div class="form-group">
                            <label>Ýsim</label>
                            <input type="text" name="Name" value="@Model.Name" class="form-control" placeholder="Ýsim giriniz" required />
                        </div>
                        <div class="form-group">
                            <label>Meslek</label>
                            <input type="text" name="Profession" value="@Model.Profession" class="form-control" placeholder="Meslek giriniz" required />
                        </div>
                        <div class="form-group">
                            <label>Resim URL</label>
                            <input type="text" name="ImageUrl" value="@Model.ImageUrl" class="form-control" placeholder="Resim URL giriniz" required />
                        </div>
                        <div class="form-group">
                            <label>Açýklama</label>
                            <textarea name="Description" class="form-control" rows="5" placeholder="Açýklama giriniz" required>@Model.Description</textarea>
                        </div>
                        <button type="submit" class="btn btn-warning">
                            <i class="fa fa-save"></i> Güncelle
                        </button>
                        <a href="/Admin/AdminTestimonial/Index" class="btn btn-secondary">
                            <i class="fa fa-times"></i> Ýptal
                        </a>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
